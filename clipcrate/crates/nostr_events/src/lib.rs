// clipcrate-nostr: Nostr event builders and relay publisher
// Provides helpers to construct and sign Nostr events (NIP-71 video events,
// NIP-57 zap receipts, etc.) and publish them to configured relays via nostr-sdk.
